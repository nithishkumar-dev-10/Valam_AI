# Dev/test-only dependencies — NOT installed in production.
# Install along with the runtime deps, e.g.:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Runs the pytest suite (tests/test_audit.py + friends).
pytest==9.1.1

# Optional PostgreSQL driver; only needed if DATABASE_URL is switched from
# sqlite:// (also usable locally / outside prod).
psycopg2-binary==2.9.10