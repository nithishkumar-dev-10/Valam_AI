.env
venv
data/plantvillage/
.DS_Store
data/deepweeds/
data/deepweeds_processed/
data/pest_data/
data/plantvillage_split.json
# Seed file contains REAL phone numbers + bcrypt hashes — never track.
data/seed/

# Runtime logs — never track (removed from the index with `git rm --cached`).
*.log
log.txt
training_log.txt

# Runtime SQLite database (backend/db/valam.db, created at runtime).
db/
